[package]
name = "irenx-edge"
version = "4.0.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
worker = { version = "0.8.5", features = ["http"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
