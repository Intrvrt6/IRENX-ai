TWELVEDATA_API_KEY=
MARKET_PROVIDER=twelvedata

# OmniRoute upstream (server-side only)
OMNIROUTE_BASE_URL=http://127.0.0.1:20128
OMNIROUTE_API_KEY=

# Optional auth for the IRENX OpenAI-compatible gateway used by OmniCopilot
# Leave empty for a private/local deployment where network access is already restricted.
IRENX_GATEWAY_API_KEY=

# 0 = let IRENX choose the best route; 1 = respect the model selected in OmniCopilot.
IRENX_COPILOT_RESPECT_MODEL=0
